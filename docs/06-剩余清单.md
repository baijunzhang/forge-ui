# 剩余清单 — HSBC AI Studio Desktop UI 改造

> 记录进度与待办,随时可查。核心功能已完成,以下为收尾 + 可选升级。

## ✅ 已完成
- 外观:中性 Codex 风、HSBC 六边形 logo、深/浅双主题、舒服字体(Inter/行距)
- diff 审阅:右侧面板、绿红逐行、✓/✕ 图标、隐藏 @@
- bug 修复:index.html 语法错误、主题切换失效、事件委托(死按钮)、面板清空、
  commit 路径不匹配、accept 反馈

## ⏳ 进行中
- 去 Commit(git)+ "Dark blue"→"Dark" + 两层文件审阅(列表→点开详情)→ 做完真机验证

## 📋 待办(优先级)

### A. 收尾(建议先清 → 得到干净可用的完整产品)
- [ ] 功能连接勾选框自动重置(**仍未修复**,重新排查中)——三个全取消立刻又全选;
      根因:某处重渲染把勾选重置回默认全选,没尊重用户选择。修法:先 console.log 定位触发者,
      让用户选择成为"真相来源",重渲染时读取并保留用户选择而非重置。

  重新排查指令(发给 AI):
  ```text
  The Feature Connections checkboxes (BBG, Outlook, Board collections) STILL auto-re-check
  themselves right after I uncheck all three — a previous attempt did not fix it. Please
  diagnose properly this time (frontend-only, keep bridge/backend intact):
  1. Find EXACTLY what re-renders or re-applies the default checked state after an uncheck
     — a periodic poll, a state refresh (getInitialState), desktopUpdateActivity, or a
     re-render of the feature-connections panel. Add a console.log to confirm the trigger.
  2. The root problem: the panel is likely REBUILT from a default state that ignores the
     user's current selection. Fix so the user's checkbox choices are the source of truth
     and are NOT overwritten on any re-render/refresh.
  3. If a refresh must rebuild the panel, have it READ the current checkbox state (or a
     saved selection variable) and re-apply THAT, not the all-checked default.
  Verify by actually unchecking all three and waiting 5 seconds — they must stay unchecked.
  Report the exact trigger and the fix.
  ```
- [ ] 去程序员黑话:`Change session: <id>`、`Validation attempts: ...shell命令` 折叠为"✓ 已检查"
- [ ] 清理测试文件 test.py / test2.py

### A2. 写入被拦(权限)——非 review 回归
- 现象:让 AI 建 test2.py 时 `Write blocked: Permission was not granted`,文件没改 → 右边无内容可 review。
  AI 还误报"已创建"。这**不是**两层 review 坏了,是**权限/守卫拦了写入**,与前端改动无关。
- 待办:查写入被拦原因(权限模式 / plan 守卫),允许写入;禁止在写入被拦时谎报成功。
- 待写入成功后,再验证两层 review:右侧文件列表 → 点文件展开绿红逐行 diff。

### A3. 自动滚动应尊重用户(流式时别强拽到底)
- 现象:AI 流式输出时,聊天区强制把用户拉回底部,想往上翻看历史被拽下去。
- 正确行为(标准聊天):用户在底部才自动跟随;一旦手动上滚就停止跟随;滚回底部再恢复。
- 纯前端。指令:
  ```text
  Bug: while the AI is streaming/running, the chat area force-scrolls me back to the bottom,
  so I can't scroll up to read earlier messages — it drags me down to where it's generating.

  Fix (frontend-only, standard chat behavior):
  1. Only auto-scroll to bottom if the user is ALREADY at (or near) the bottom.
  2. As soon as the user scrolls UP during streaming, STOP auto-scrolling and let them read
     freely — do not yank them back down.
  3. Resume auto-follow only when the user scrolls back to the bottom themselves.
     (Optional: show a small "Jump to latest" button while they're scrolled up.)

  Impladvice: track a "stickToBottom" flag; set it false on manual scroll-up, true when the
  user is within ~40px of the bottom. Gate the auto-scroll on that flag.
  Verify by scrolling up while the AI is generating — the view must stay where I put it.
  ```

### A4. Review changes 面板顶部黑色 bar（视觉不一致，已实测发现）
- 现象:打开 "Review changes"(diff 审阅面板)时,顶部有一条突兀的黑色/深色 bar,跟已经
  统一好的中性浅色主题不搭,看起来像没做完。已通过真机截图确认存在。
- 待办:先诊断这条 bar 到底是什么(原生窗口标题栏 / 自定义 header div / 遗留旧样式),
  再按诊断结果修,不要直接猜配色乱改。

  指令(发给 AI):
  ```text
  STEP P7 — Fix the unstyled dark bar above "Review changes". Investigate first, then fix
  to match the existing neutral theme. Do NOT touch bridge/backend or existing diff logic.

  INVESTIGATE (report before changing anything):
  1. When "Review changes" opens, is it: (a) a separate native OS/Qt window with its own
     title bar, (b) a modal/dialog overlay within the same window, or (c) a panel that
     slides in and pushes the existing layout (as it visually appears in my screenshot —
     same input box and taskbar still visible below/beside it)?
  2. If it's a native window frame (Qt's default titlebar), report whether the window was
     created with a frame (default OS chrome) or frameless — this determines whether we can
     restyle it directly with CSS or need a custom titlebar widget instead.
  3. If it's a custom header/div, find its class/ID and current background color/rule, and
     report why it's black (hardcoded color? leftover from before the neutral-theme pass?
     inherited from an old dark-blue variable that was never migrated?).
  4. Check whether this same black bar appears elsewhere (e.g. above the main chat window
     too, per my screenshot) or only above the Review changes panel specifically.
  Wait for my go-ahead before fixing — tell me which of the above it is.

  FIX (once diagnosed — this is the expected direction, adjust based on what you find):
  - If it's a custom header div: restyle its background to match the existing "surface"
    color used elsewhere (dark #202020 / light #ffffff), border-bottom 1px at 8% opacity
    (rgba(255,255,255,.08) dark / rgba(0,0,0,.08) light) instead of a hard black block.
    Title text should use the existing primary text color, 14px, weight 500 — not white
    text on black.
  - If it's a native OS titlebar we can't easily restyle: check whether the main app
    window already uses a frameless/custom titlebar (it likely does, since the rest of
    the chrome looks custom) — if so, apply that SAME custom titlebar component/style to
    this window/panel too, for visual consistency, rather than leaving the OS default.
  - Either way: the close button ("×" top-right) should match the app's existing icon
    button style (monochrome stroke icon, subtle hover background), not sit inside a
    black strip on its own.

  Verify live: open Review changes in both light and dark themes, confirm the bar matches
  the rest of the app's chrome (no jarring black strip), and confirm the panel still opens/
  closes/reviews diffs correctly. Report what the black bar actually was.
  ```

### A5. 主窗口原生深蓝色标题栏/边框（跟 A4 同类问题，真机截图确认）
- 现象:不只是 Review changes,主窗口本身的标题栏和边框也是系统默认深蓝色,没有被中性主题
  覆盖,大概率是主窗口没做自定义标题栏(frameless),Windows 在用系统主题色画自己的原生边框。
- 风险比 A4 更高:很可能不是纯 CSS 能改的,得动 Python/Qt 那边创建窗口的代码(frameless +
  自绘标题栏,还要保留拖动/双击最大化)。建议放在 Quote(P8/P9)和 P0-P4 之后再单独做,
  跟纯前端的东西分开发,别混在一起。

  指令(发给 AI):
  ```text
  STEP P7b — The unstyled native window chrome also appears on the MAIN window, not just
  Review changes: it currently shows the default OS blue title bar / border (see
  screenshot), which clashes badly with the neutral in-app theme. This is very likely the
  SAME root cause flagged in P7's investigation question #4 — confirm that first.

  INVESTIGATE:
  1. Confirm: is the main application window created as a standard framed OS window
     (e.g. plain QMainWindow with no custom title bar), which is why Windows is drawing
     its own default blue-accented title bar and border around it? Report where this
     window is created in the code (file/line).
  2. Report whether ANY window/panel in this app already uses a frameless/custom title
     bar (per P7's finding) — if something already has custom chrome, reuse that exact
     same approach here for consistency, rather than inventing a second pattern.

  FIX (pick based on what you find, tell me which you're doing and why):
  - Preferred: make the main window frameless (Qt.FramelessWindowHint or platform-
    appropriate flag) and add a custom title bar matching the in-app theme: height ~40px,
    background = surface color (dark #202020 / light #ffffff), bottom border 1px at 8%
    opacity, app name in 13px/weight 500 primary text color on the left, and standard
    minimize/maximize/close buttons on the right styled as simple monochrome glyphs (not
    OS-default colored icons) with a subtle hover background (rgba(255,255,255,.06) dark /
    rgba(0,0,0,.04) light). Must still support drag-to-move (implement hit-testing on the
    custom bar) and double-click-to-maximize — don't lose these.
  - Fallback (smaller change, if frameless is too risky right now): if the platform
    supports it (Windows 11 via DWM APIs, e.g. Qt 6.5+'s title bar color APIs), just
    recolor the native title bar/border to a neutral color instead of the OS default blue,
    without going fully frameless. Tell me if the Qt/PySide version in use supports this.

  Do NOT touch the bridge, IDs, event handlers, or any existing feature logic — this is
  purely about window chrome. Work on a checkpoint commit first since this touches the
  app shell, not just CSS. Verify live: relaunch the app, confirm the title bar/border is
  neutral (no default OS blue) in both light and dark themes, and confirm dragging,
  minimizing, maximizing, and closing all still work normally.
  ```

### A6. Diff 代码超长行被截断,看不到完整内容
- 现象:Review changes 里长代码行(长函数签名/字典字面量等)在面板边缘被截断,没法看到
  剩下的内容,截图里能看到行尾直接消失(如 "dis..."、"pending_ima...")。

  指令(发给 AI):
  ```text
  STEP P10 — Diff code lines are cut off; make them scrollable.
  Frontend-only CSS/JS, do not touch bridge/backend/diff data.

  Problem: in the Review changes diff panel, long code lines (e.g. long function
  signatures/dict literals) are truncated at the edge of the panel with no way to see
  the rest of the line — see screenshot, e.g. lines ending in "dis...", "pending_ima...".

  INVESTIGATE first:
  1. Find the CSS rule currently applied to the diff code container/rows — is
     `overflow` set to `hidden`/`clip` (which is why long lines vanish), or is
     `white-space: nowrap` used without any scroll container around it?
  2. Check if there's a horizontal scrollbar already present but just invisible/broken
     (e.g. zero height, wrong z-index, clipped by a parent's overflow:hidden).

  FIX:
  1. Make each diff row's code content horizontally scrollable: the code text stays on
     one line (white-space: pre, no wrapping — wrapping breaks diff alignment), and the
     row's container gets `overflow-x: auto`.
  2. Style the horizontal scrollbar to match the neutral theme instead of the OS
     default: thin (6-8px), track transparent, thumb = rgba(255,255,255,.15) dark /
     rgba(0,0,0,.15) light at rest, rgba(255,255,255,.3) dark / rgba(0,0,0,.3) light on
     hover, border-radius 4px. Only show the scrollbar when hovering that row (or keep
     it always faintly visible if that's simpler — your call, tell me which).
  3. Alternative for very long lines: consider a "wrap long lines" toggle button in the
     diff panel header, off by default (diffs read better unwrapped), so the user can
     opt into wrapping instead of scrolling if they prefer. Optional — skip if it adds
     too much complexity, scrolling alone is the must-have.

  Verify live: open a diff with a genuinely long line, confirm you can scroll
  horizontally to read the full line, in both light and dark themes.
  ```

### A7. 所有滚动条拖动不丝滑
- 现象:文件列表、diff 面板、聊天区等多处滚动条,拖动时手感卡顿/跳动,不跟手。

  指令(发给 AI):
  ```text
  STEP P11 — Scrollbar dragging feels janky across the app; make it smooth.
  Frontend-only, investigate before fixing.

  INVESTIGATE first:
  1. Are the scrollbars in this app native browser scrollbars styled via CSS
     (::-webkit-scrollbar-thumb etc.), or a custom JS-driven scrollbar component that
     recalculates position on mousemove?
  2. If custom/JS-driven: check whether the thumb or track has any CSS `transition`
     property (e.g. `transition: all 0.2s`) applied — a transition on a property being
     updated in real time during drag is the single most common cause of laggy-feeling
     scrollbar drags, because each mousemove fights the transition's easing instead of
     moving instantly.
  3. If custom/JS-driven: check whether the drag handler updates scroll position
     directly on every `mousemove` event (can cause jank if not batched) versus using
     `requestAnimationFrame` to batch updates to the next paint frame.
  4. Identify EVERY place a custom scrollbar is used (file list, diff panel, chat feed,
     sidebar sessions list, etc.) — list them so a fix can be applied consistently
     everywhere, not just one.

  FIX (apply once diagnosed, likely some combination of):
  1. Remove any `transition`/`animation` on scrollbar thumb position, width, or
     transform during active dragging (transitions are fine on hover/color changes,
     not on position while the user is actively dragging).
  2. If drag position is computed on every mousemove without batching, wrap the
     position update in `requestAnimationFrame` so it's synced to the browser's paint
     cycle instead of firing faster than the screen can render.
  3. Make sure `user-select: none` is applied to the scrollbar thumb/track while
     dragging, so text selection doesn't fight with the drag gesture (a very common
     cause of "sticky"/jumpy scrollbar drags).
  4. If these are QWebEngineView-rendered scrollbars, also check whether hardware
     acceleration / compositing is enabled for the web view — software rendering can
     make even correct CSS/JS feel laggy. Report if this is configurable.

  Verify live: drag every scrollbar you found in step 4 (file list, diff panel, chat
  feed, sidebar) slowly and quickly, confirm the thumb tracks the cursor 1:1 with no
  lag or snapping, in both light and dark themes.
  ```

> **更正(重要)**:之前把 P8/P9 Quote 功能归类为"纯前端、不碰后端"——实测截图(`desktop.py`
> 的 diff)显示它其实动了后端桥接代码(`pending_quote_payload`、`_normalize_pending_quote`),
> 说明引用的文本需要经过 Python 桥接层才能带到 AI 那边,并不是纯前端能独立完成的。
> 已知这条分类判断错了,记录于此,以后类似"是否纯前端"的判断要以实测代码 diff 为准,
> 不要只看 prompt 里写的"frontend-only"就当真——AI 执行时可能会因为实际需要而越界。

> **✅ P8/P9 Quote 已验证跑通**(真机截图确认):引用文字 → quote block → 续问 → 后端
> `py_compile` 通过;P8 的约束也保留住了(streaming/running 中的消息不能 quote,只能
> quote 已完成的消息)。这是目前清单里第一个端到端验证通过的 Phase 3 功能。

### A8. 消息气泡改造:去标题/去边框,背景改成暖米色(参考 Claude 自己的界面)
- 用户明确要求参考 Claude(本对话界面)的视觉风格:AI 回复不要标题、不要边框方框;
  user 消息框现在有黑色描边,要去掉;整体背景要从偏冷的白/浅灰改成暖米色调。

  指令(发给 AI):
  ```text
  STEP P12 — Redesign the chat message layout to be borderless and warm-toned, matching
  a Claude/Codex-style feed instead of boxed chat bubbles. Frontend-only, CSS/markup only
  — do not touch bridge, event handlers, or message data structures.

  1. Remove the "AI" / assistant title/label that currently sits above each AI reply, and
     remove the bordered/boxed container around the AI reply text. The AI's response
     should render as plain flowing text directly on the background — no box, no border,
     no header label. (If a small identity marker is still wanted, use a tiny 16px
     monochrome icon to the left of the FIRST line only, not a text title on every
     message — but removing it entirely is the preferred, cleaner option; use your
     judgment and tell me which you did.)

  2. Remove the border/outline ("black line") currently drawn around the user's message
     box. The user's message keeps a soft filled background (see palette below) but NO
     visible border/stroke around it.

  3. Update the light-theme background tokens to a warmer, cream tone (closer to how
     Claude's own interface looks) instead of the current plain #fafafa:
     - App background: #F7F5F0 (warm cream, not pure white/grey)
     - Sidebar/secondary surface: #F1EEE6 (very slightly darker cream than the main bg,
       just enough for subtle separation)
     - User message background: #ECE8DE (a soft warm tan block, rounded corners ~10px,
       generous padding 10px 14px, NO border/outline)
     - Primary text: #33302A (warm near-black, not pure #000)
     - Secondary/muted text: #7A756A (warm grey)
     - Borders (where still needed, e.g. between sidebar sections): 1px
       rgba(0,0,0,.06) — even fainter than before, since the warm palette already
       creates enough separation without hard lines.
     Leave the DARK theme tokens as they already are (neutral charcoal) — this warm
     cream treatment is for light mode only, matching typical Claude/Codex light themes.

  4. AI message text: no background, no border — just the app's background color behind
     it, comfortable line-height 1.6, generous top/bottom spacing between turns (16-20px)
     so consecutive messages don't feel cramped without a box to separate them.

  5. Do not change the neutral monochrome button/accent rules already established
     elsewhere (New chat, Send, etc. stay monochrome) — this step is about the message
     feed's background/bubble treatment only, not accent colors.

  Verify live in light mode (primarily) and confirm dark mode still looks correct/
  unaffected. Screenshot both and report what you removed vs kept.
  ```
  > 注:这套暖米色数值是"参考 Claude 界面观感"给的近似值,不是像素级精确提取,能接受
  > AI 微调到看着舒服为止,不用死抠这几个 hex 值。

### A9. Permission Required 弹窗好看化(复用 P6 的弹窗视觉系统)
- 现象:app 里已有一个 "Permission Required" 弹窗,样式跟其他地方不统一,不好看。
- 做法:不用重新设计一套,直接复用之前 P6(跨文件夹权限弹窗)定好的视觉规范,保证
  app 里所有"权限类弹窗"长得一致,而不是每个各画各的。

  指令(发给 AI):
  ```text
  STEP P14 — Restyle the existing "Permission Required" dialog to match the app's
  neutral design system already used elsewhere (reuse the shared dialog visual language
  from the folder-access permission dialog if that component already exists — do not
  build a second, differently-styled dialog system). Frontend-only, do not change what
  triggers it or its underlying logic, only its appearance.

  Visual spec (same as the folder-access dialog):
  - Overlay: rgba(0,0,0,.45) scrim, backdrop-filter blur(2px) if supported.
  - Card: centered, max-width 420px, border-radius 12px, background = elevated surface
    (dark #262626 / light cream #FFFDF9), border 1px at 8% opacity, soft shadow only
    (0 8px 30px rgba(0,0,0,.2), no hard shadow).
  - Icon: small monochrome outline icon (lock/shield — whichever fits "permission") in
    a 48px circle with a faint neutral background — no red/yellow warning color, keep
    it calm and neutral like the rest of the app; the text carries the "pay attention"
    weight, not alarm color.
  - Title 15px/weight 500, body 13px secondary color, line-height 1.6.
  - If it shows a command/path/action being requested, put it in a monospace inline
    chip (JetBrains Mono, subtle background, rounded 4px) like other technical details
    elsewhere in the app — not raw unstyled text.
  - Actions: same Allow/Deny (or Approve/Cancel) button treatment as the rest of the
    app — primary = existing monochrome primary button style, secondary = ghost/outline
    button. No colored "danger" button unless denying is genuinely destructive.

  Verify live: trigger the real Permission Required flow, confirm it now matches the
  rest of the app visually in both themes, and confirm Allow/Deny still work exactly as
  before (styling-only change).
  ```

### A10. composer 区域整体简化,改成 Claude 的做法(功能全保留,只改布局)
- 现状:composer 上方常驻一整块 FOLDER / PERMISSIONS / MODE 行 + 展开的 "CONNECTED
  CAPABILITIES" 面板(BBG / Outlook mail+Calendar / Board collections 勾选框 + Browse
  boards/mail)+ 底部 Attach files / Setup / Connectors 4 · Features 0 · Boards 0 这行
  计数,常驻占用很大空间,信息全部平铺展示。
- 目标:参考 Claude 自己界面的做法——底部只留一行紧凑控件(Mode 胶囊 + "+" 按钮 + 可选
  下拉),"+" 点开菜单里才展开 Add files/Add folder/Connectors 等具体功能,Connectors
  再点开是第二层子菜单(每个连接器一个 toggle 开关,而不是一堆常驻勾选框)。
- **功能不能丢**,只是把"常驻大面板"收进"按需展开的紧凑菜单"里:
  - FOLDER(工作目录选择)—— 保留
  - PERMISSIONS(accept-all 等权限模式)—— 保留
  - MODE(Coding 等任务模式)—— 保留
  - BBG(Bloomberg Desktop API)connector —— 保留
  - Outlook mail + Calendar connector —— 保留
  - Board collections connector —— **明确要删掉**,不用保留
  - Attach files —— 保留
  - Setup —— 保留(可以合并进 "+" 菜单或 Manage connectors 里)
  - 语音/麦克风功能 —— **不需要**,即使 Claude 参考图里有麦克风图标,也不用做这个功能,
    "+" 旁边不用加 mic 按钮

  指令(发给 AI):
  ```text
  STEP P15 — Simplify the composer area to match Claude's compact pattern, WITHOUT
  removing any existing functionality except Board collections (explicitly being
  dropped). Frontend-only, CSS/markup/JS — do not touch bridge, backend logic, or how
  folder/permissions/mode/connector settings are actually applied, only how they're
  presented.

  Current state (see reference screenshots): the composer has an always-expanded block
  above the text input — a FOLDER / PERMISSIONS / MODE row, a "CONNECTED CAPABILITIES"
  panel with checkboxes for BBG, Outlook mail + Calendar, and Board collections, plus a
  footer showing "Attach files · Setup · Connectors 4 · Features 0 · Boards 0". This
  takes up a lot of permanent vertical space and shows everything flat, all the time.

  Target layout (like Claude's own composer):
  1. The text input itself becomes the dominant, clean element — a plain bordered
     textarea/input, no panel clutter above it.
  2. Below the input, ONE compact row of controls:
     - A "Mode" pill/dropdown (or two small pills if you keep Permissions and Mode as
       distinct concepts — do NOT force them into one dropdown if they control
       different things; use your judgment, but keep whichever pills there are small
       and unobtrusive, matching the existing neutral button style already used
       elsewhere in the app).
     - A "+" button that, when clicked, opens a menu with:
         - Add files or photos
         - Add folder (this is for attaching a folder as context to THIS message —
           distinct from the FOLDER path that sets the whole session's working
           directory; keep both concepts, just don't show the working-directory
           FOLDER picker as a big always-visible row — fold it into this same "+"
           menu or the Mode area, whichever fits better structurally)
         - Connectors (opens a second-level submenu on hover/click, matching Claude's
           pattern): list BBG (Bloomberg Desktop API) and Outlook mail + Calendar as
           TOGGLE switches (on/off), not checkboxes in a panel. Do NOT include Board
           collections here — remove it and its "Browse boards" / board-related UI
           entirely (confirm with me if anything else depends on it before deleting).
         - A "Manage connectors" / "Setup" entry at the bottom of the Connectors
           submenu (merge the old separate "Setup" button into here).
     - Do NOT add a microphone/voice input button — skip that part of Claude's
       reference UI entirely, it's explicitly not wanted right now.
  3. Remove the permanent footer line ("Attach files · Setup · Connectors 4 · Features
     0 · Boards 0"). If a lightweight status hint is still useful, a small subtle badge
     (e.g. a number on the "+" icon or on "Connectors" showing how many are active) is
     fine — keep it minimal, not a full text row.
  4. Styling: reuse the app's existing neutral theme tokens (surfaces, borders, text
     colors already established) — the "+" menu and its submenu should look like
     lightweight popovers (small shadow, rounded 8-10px corners, 1px border at low
     opacity), not full modal dialogs.

  Preserve all existing behavior/wiring for folder selection, permission mode, task
  mode, BBG connector, Outlook connector, and attach-files — this is a presentation/
  layout change only. Verify live: confirm every one of these still works exactly as
  before after being moved into the new compact menu, in both light and dark themes,
  and confirm Board collections and its UI are fully gone (no orphaned "0 boards"
  leftovers).
  ```

### A11. composer 输入框和控制行要合并成一个整体卡片(参考 Claude)
- 现状:文本输入框是一个单独的圆角框,下面的控制行(+ / accept-all / Coding / 文件夹
  chip / Send)是另一条视觉上分离的行,两者看起来是"两块拼在一起"而不是一体的。
- 目标:参考 Claude 的做法——文本框和下面的控制行被包在**同一个**圆角边框卡片里,
  中间没有第二条边框或明显缝隙,读起来是一个连贯的组件。

  指令(发给 AI):
  ```text
  STEP P18 — Unify the composer textarea and its control row into ONE bordered card,
  matching Claude's own composer structure. Frontend-only CSS/markup, do not touch
  bridge/backend or any existing button wiring — this is purely a visual restructure.

  Current state: the text input has its own rounded border/box, and the control row
  below it (+, accept-all, Coding, folder chip(s), Send) is a visually separate row
  with a gap — it reads as two stacked-but-disconnected elements.

  Target (see reference screenshots): ONE outer container — rounded corners (14-16px),
  1px border at low opacity (rgba(255,255,255,.08) dark / rgba(0,0,0,.08) light),
  background = surface color — that contains BOTH the textarea AND the control row
  stacked vertically inside it, with no second border and no visible gap between them
  (at most a very subtle internal divider if truly needed, but Claude's reference has
  none — prefer spacing alone).

  1. Remove the textarea's own separate border/background; it becomes transparent and
     sits directly inside the new outer card, full width, comfortable padding (12-14px).
  2. The control row (Mode/permission pills, "+", folder chip(s) from P17) sits
     directly below the textarea, INSIDE the same card, with its own internal padding
     (8-10px) — reuse the exact pill/button styles already established in A10/P17, do
     not invent new ones.
  3. Send action: match Claude's placement — a small icon-only button (paper-plane or
     return-arrow glyph, monochrome, no text label) inside the TOP-RIGHT corner of the
     textarea's own row (not a separate large labeled "Send" button off in the control
     row). If moving it feels too disruptive to existing layout code, keep Send as an
     icon-only button (not a big blue labeled button) at the right end of the control
     row instead — tell me which you did and why. Either way, remove the large colored
     "Send" button style in favor of a small monochrome icon button, consistent with
     the rest of the app's restrained button language.
  4. The whole card should have a subtle focus state: border becomes slightly more
     visible (e.g. 12% opacity instead of 8%) when the textarea inside it has focus,
     so the user gets a clear but calm "this is active" cue without a harsh blue OS
     default outline.

  Verify live in both light and dark themes: confirm the textarea and control row now
  read as one seamless card, typing/sending/mode-switching/folder-chip interactions
  all still work exactly as before, and there's no leftover double-border or visible
  seam between the two areas.
  ```

### A12. 左下角 Studio/Sign-in 区域太杂乱,需要重新梳理
- 现状(见截图):侧边栏左下角挤了一堆不相关的东西——STUDIO/DESKTOP 标签、
  `http://localhost:8080` URL 输入框、"Sign in" 大黑按钮和 "Dark" 按钮并排放在一起
  (一个是登录 Studio,一个是切主题,语义完全不相关却挤在同一行)、"Signed in" 状态字、
  "78 turns · 日期" 统计、还有一行看起来像调试日志残留的文字("Loaded session:
  STEP P3 — ... Placem...")。整体没有清晰的信息层级,看着很杂乱。
- 目标:参照整个项目已经建立的中性 Claude 风格,把这个角落梳理清楚:
  1. Studio 连接(URL + Sign in + 连接状态)自成一组,视觉上跟其他东西分开
  2. 主题切换("Dark"/"Light")跟 Studio 登录是不同性质的操作,不应该并排放,建议挪到
     侧边栏顶部(靠近 "AI Markets Desktop" 标题旁的 ☰ 菜单区域),做成小图标切换
     (太阳/月亮),而不是文字按钮占着登录区的位置
  3. "N turns · 日期" 和那行调试日志文字,大概率是可以精简或去掉的冗余信息(sessions
     列表本身已经显示 turns 数了)——但这条要先问清楚 AI 那行日志是不是故意留的调试
     信息,不要自己瞎猜就删

  指令(发给 AI):
  ```text
  STEP P19 — Investigate first, then redesign the bottom-left Studio/Sign-in corner of
  the sidebar. Frontend-only for the visual redesign; investigate before touching
  anything that might be load-bearing (e.g. the log-looking line).

  INVESTIGATE:
  1. What does the "Dark" button actually do — is it a global light/dark theme toggle?
     Confirm it's unrelated to Studio sign-in (just happens to sit next to it today).
  2. What is the line that looks like "Loaded session: STEP P3 — Running indicator on
     sessions list. CSS/frontend-only. Placem..." — is this a genuine debug/status
     message intentionally shown to the user (e.g. confirms which session loaded), or
     leftover console-log-style text that ended up rendered in the UI by accident?
     Report which, and where in the code it comes from.
  3. Is "N turns · <date>" here redundant with the turn-count/date already shown per
     session in the Sessions list above, or does it represent something different
     (e.g. the currently active session specifically vs. the list in general)?
  Wait for my go-ahead on what to keep vs. remove/relocate based on these findings.

  REDESIGN (once confirmed):
  1. Move the theme toggle OUT of this corner — relocate it near the ☰ menu icon at
     the top of the sidebar (next to "AI Markets Desktop"), as a small icon-only
     toggle (sun/moon glyph, monochrome, no text label), since it's a global app
     setting unrelated to Studio sign-in.
  2. Group the Studio connection into one small, self-contained block at the bottom
     of the sidebar, separated from the sessions list above by a subtle top border
     (1px at 8% opacity) or just extra spacing — reuse the existing uppercase muted
     section-label style (12px, letter-spacing .04em, muted color) for a "STUDIO"
     label, matching how "SESSIONS" is styled.
  3. Studio URL: a compact, single-line input, existing neutral input styling (not a
     bare unstyled text field) — border 1px at 8% opacity, 13px text, padding 6px 10px.
  4. Connection status: a small dot + label inline ("Connected" / "Not signed in"),
     monochrome by default; if you want the dot to carry meaning at a glance, a small
     neutral green for "connected" / grey for "not connected" is acceptable here since
     it's a genuine status indicator (not decorative) — keep it subtle either way.
  5. "Sign in" button: restyle to the same small monochrome primary-button style used
     elsewhere in the app (not a large standalone black button) — right below or
     beside the URL input, whichever reads more naturally as one compact unit.
  6. Only keep the turn-count/date and the "Loaded session…" log line if P19's
     investigation confirmed they're meaningful and not redundant/accidental — if kept,
     style them as small muted secondary text (12px, muted color), not full-width lines
     competing visually with the Studio block above them.

  Verify live in both themes: confirm Studio sign-in/connection still works exactly as
  before, the theme toggle still works from its new location, and the corner now reads
  as one tidy block instead of several unrelated things crammed together.
  ```

### A13. 权限确认弹窗现在是系统原生框,要换成自己的样式
- 现象:至少两个场景会弹权限确认——(1) manual/ask-permission 模式下,AI 要改代码时
  弹出"允不允许"确认;(2) AI 要访问另一个文件夹时(对应 P5/P6)。这两个现在弹出来的都是
  **系统默认的原生对话框**(不是 app 自己画的),字体/配色/边框全是 OS 默认样式,跟 app
  整体的中性 Claude 风格完全不搭。
- 关键点(参考 A5 的教训):**如果这是真正的原生系统弹窗(比如 Qt 的 QMessageBox),CSS
  是改不动的**——原生弹窗是操作系统画的,不是网页内容,跟改窗口标题栏是同一类问题。
  真正的解法通常是**把原生弹窗调用整个换掉,改成 app 自己在 webview 里画的自定义弹窗**
  (P6 已经给过一套完整的视觉规范,A9 也已经把"Permission Required"弹窗复用了这套——
  这次要确认的是:code-edit 权限确认和 folder-access 权限确认,是不是也能复用同一套,
  而不是三个权限弹窗各自长得不一样)。

  指令(发给 AI):
  ```text
  STEP P20 — Investigate first: are the code-edit permission prompt (shown in manual/
  ask-permission mode before applying a change) and the folder-access permission prompt
  (P5/P6) currently native OS dialogs (e.g. a Qt QMessageBox or platform-native
  message box), or already custom HTML/CSS dialogs that just need re-styling?
  - Report exactly what API/call triggers each one and where in the code.
  - Report whether a shared custom dialog component already exists from the
    "Permission Required" restyle (A9) that could be reused for these two as well,
    rather than building three separate dialog implementations.
  - If either is a genuine native OS dialog, confirm it CANNOT be restyled with CSS
    (same root cause as the native title bar issue) and that the real fix is
    replacing the native call with a custom in-app modal rendered in the webview.
  Wait for my go-ahead before changing anything.

  STEP P21 — Replace native permission dialogs with ONE shared custom dialog component,
  reused for all three permission scenarios (Permission Required / code-edit approval /
  folder-access approval) so they all look identical and match the app's theme —
  reuse the exact visual spec already defined for the folder-access dialog (P6):
  - Overlay: rgba(0,0,0,.45) scrim, backdrop-filter blur(2px) if supported, no
    click-outside-to-dismiss (these are security/approval decisions).
  - Card: centered, max-width 420px, border-radius 12px, background = elevated surface
    (dark #262626 / light #FFFDF9), border 1px at 8% opacity, soft shadow only.
  - Icon: small monochrome outline icon in a 48px circle with a faint neutral
    background (lock/shield for permissions, folder for folder-access) — no red/yellow
    alarm color, keep it calm; vary ONLY the icon/title/body text per scenario, not the
    shell.
  - For the code-edit scenario specifically: show what's being changed — file path in
    a monospace inline chip, and if feasible a compact one-line summary (e.g. "+12 −4
    lines") rather than dumping a full diff into the dialog (the full diff review
    already lives in the Review changes panel).
  - Actions: Allow/Deny (or Approve/Reject) using the app's existing monochrome
    primary/ghost button styles — no colored "danger" button.
  - Keyboard: Esc = Deny, Enter = Allow, matching P6.

  This must actually replace the underlying native call (not just add a styled overlay
  on top of a native dialog that's still there) — the native OS dialog should no longer
  appear at all for any of these three scenarios.

  Verify live: trigger all three scenarios for real (Permission Required, a manual-mode
  code edit, and an out-of-scope folder access) and confirm they all render the SAME
  custom dialog shell with only the icon/copy differing, in both light and dark themes,
  and that Allow/Deny still function correctly for each.
  ```

### A14. Search messages 输入框:黑色聚焦边框 + 重复的两个 "×"
- 现象(见截图):在 sessions 顶部的搜索框里打字(比如 "hel")时,输入框会出现一圈突兀的
  黑色边框,而且右边同时出现**两个**清除("×")按钮,不是重复就是一个原生一个自定义的
  叠在一起了。

  指令(发给 AI):
  ```text
  STEP P22 — Fix the search-messages input: remove the black focus border, and remove
  the duplicate clear ("×") button so only one remains. Frontend-only CSS/JS.

  INVESTIGATE first (quick, this is likely simple but confirm before guessing):
  1. The black border on focus — is this the browser/webview's default `outline` on
     `:focus` (never overridden), or an explicit `border: 1px solid black` /
     similar rule applied to this specific input?
  2. The two "×" buttons — is one of them the browser-native clear button that some
     webviews render automatically for `<input type="search">` (via
     `::-webkit-search-cancel-button`), while the OTHER is a custom clear button you
     built in HTML/JS? Confirm which is which before removing one.

  FIX:
  1. Replace the default focus outline with the same calm focus treatment used
     elsewhere in the app (e.g. the composer card in A11): border goes from the
     resting 1px/8%-opacity neutral border to a slightly more visible ~12-15% opacity
     neutral border on focus — no black ring, no OS-default blue ring either.
  2. Keep exactly ONE clear button. If the duplicate is the native
     `::-webkit-search-cancel-button`, hide it via
     `input[type="search"]::-webkit-search-cancel-button { display: none; -webkit-
     appearance: none; }` (or switch the input to `type="text"` if simpler) and keep
     only your custom "×" button, styled as a small monochrome icon button matching
     the rest of the app (subtle hover background, no border).

  Verify live: focus the search input (confirm calm neutral focus ring, no black
  border), type something (confirm exactly one "×" appears and clears the field when
  clicked), in both light and dark themes.
  ```

### A15. "AI 正在思考"的加载动画太生硬(三个不怎么动的实心点)
- 现象:AI 开始生成回复时,当前显示三个实心圆点,但动画很弱,基本看不出在动,不像常见
  聊天工具那种"依次跳动"的打字指示器,感觉像卡住了。
- 这跟 P1(任务时间线)/P2(状态小标签)不冲突——即使以后做了 P1/P2,发消息后到第一个
  活动事件抵达之前,通常还是会有一个短暂的"完全没数据"的瞬间,这个加载动画就是补那个
  瞬间用的,值得现在先单独做掉。

  指令(发给 AI):
  ```text
  STEP P23 — Fix the "AI is thinking" loading indicator: the current three dots barely
  animate. Replace with a proper staggered typing-indicator animation. Frontend-only
  CSS/JS.

  INVESTIGATE first (quick): confirm whether the dots currently have NO animation at
  all, or a very subtle one that's just hard to notice (e.g. wrong timing/easing) —
  report which, so the fix targets the real cause.

  FIX:
  1. Classic staggered dot animation: 3 dots, each 6-8px, monochrome (secondary/muted
     color — #a0a0a0 dark / #666 light), spaced 4-6px apart. Each dot animates opacity
     0.3 → 1 → 0.3 (or a subtle 1 → 1.3 → 1 scale, pick whichever looks calmer) over
     ~1s, ease-in-out, infinite loop — but with a staggered `animation-delay` per dot
     (0ms, 150ms, 300ms) so they animate in sequence like a wave, not all together.
     This is the standard iMessage/Slack-style typing indicator — the key fix is the
     STAGGER, since 3 dots pulsing in perfect unison reads as "barely moving."
  2. Reuse the same breathing keyframe timing already established for the P1/P2/P3
     activity dots (so all "something is happening" indicators in the app feel like
     one consistent animation language, not several different ones).
  3. Optional addition (do this too if it's not much extra work, otherwise skip and
     tell me): pair the dots with a small "Thinking…" label in muted text, OR give the
     label itself a subtle left-to-right shimmer (a soft gradient sweep across the
     text, ~1.5s loop) instead of/in addition to the dots — similar to Claude's own
     "thinking" state. Your call which combination looks best; show me both if easy.

  Verify live: send a message, confirm the loading indicator now clearly reads as
  "actively working" (obvious staggered motion, not a near-static blob), in both light
  and dark themes.
  ```

### A5 补充:左边缘的深色竖条
- 追加确认:深蓝色边框问题里,左边缘那条竖直的深色条也要一起处理,不只是顶部标题栏。
  发 A5 的 STEP P7b 时,在调查第 1 条后面追加这句即可:
  ```text
  Also specifically check the LEFT edge of the main window — there is a visible dark
  vertical strip there distinct from the top title bar. Report whether it's the same
  native-frame root cause or a separate element (e.g. a leftover sidebar-accent border
  or an unstyled resize-handle strip).
  ```

### 更多"变高级"的可选想法(未写成完整 prompt,想要哪个告诉我,我再展开)
- 代码块:等宽字体 + 语法高亮 + hover 时右上角出现小 "Copy" 按钮
- 新消息/流式文字出现时用轻微淡入(8px 位移 + 透明度,150-200ms),不要生硬地"啪"一下出现
- 空状态(没选中任何 session / 新建对话时):居中的柔和占位符(小图标 + 一行灰字),
  不要留一片空白
- 输入框聚焦时用细的中性描边,不要 Windows 默认的蓝色系统 focus 框
- 全局按 4px/8px 网格统一间距,避免东一块西一块的随意留白
- "Reasoned" 折叠区(已经有这个模式,很好,跟 Claude Code 一致)可以再加:展开/收起时
  chevron 图标平滑旋转,展开内容用斜体灰字弱化处理

### B. Phase 3(锦上添花,纯前端,有余力再做)
- [ ] 任务时间线:读文件→计划→修改→测试→总结
- [ ] 状态小标签:正在读文件 / 正在跑测试 / 等待批准
- [ ] 运行中会话的小圆点
- [ ] 侧边栏文件夹分组
- [ ] 跨文件夹访问权限弹窗(P5/P6,新增,可能碰后端守卫)
- [ ] 引用/quote 历史消息里的一段,续问(P8/P9,新增,纯前端)
- [ ] 顶部"+文件夹"chip,支持添加多个本地文件夹(P16/P17,新增,可能碰后端)

用法:P0 先发→等回报确认没问题→再依次发 P1→P2→P3→P4,一步一步来,不要一次全丢。
P5/P6、P8/P9、P16/P17 都是独立的新功能线,做完 P0-P4 之后再单独走;各自先发调查步,
再发实现步（历史教训见 A2:"权限/守卫层"的行为经常跟前端表现不一致,得先查清楚再设计,
否则容易做出一个好看但没接对真实逻辑的假功能）。P16/P17 跟 P5/P6 是同一主题(多文件夹
访问),建议一起看,别分开孤立地做。

配色系统统一说明(以下所有 prompt 都复用这套,已在项目里落地):
深色 bg #1a1a1a / surface #202020 / elevated #262626 / text #e8e8e8 / secondary #a0a0a0 /
muted #6e6e6e / border rgba(255,255,255,.08)。
浅色 bg #fafafa / surface #fff / text #1a1a1a / secondary #666 / border rgba(0,0,0,.08)。
强调色极少:主按钮黑白单色,绿/红只留给 diff 增删行;字体 Inter,行距 1.6,字重只用 400/500。

#### STEP P0 — 先调查,不要改代码
```text
STEP P0 — Investigate first (do NOT edit yet):
- Report exactly what activity/status events the frontend already receives (the app
  already shows tool cards like Read / Grep / Bash / Write with statuses). List:
  1. The event name/channel (e.g. desktopUpdateActivity) and its payload shape
     (fields, types, example JSON).
  2. Where in the DOM/JS these are currently rendered (function name, file, line).
  3. Whether there's already a concept of "current session id" / "active turn" I can
     key a timeline off of.
- I want to build the timeline/status/running-dot purely from these EXISTING events,
  no backend change. If something needed is missing, tell me exactly what instead of
  faking it with a placeholder.
Wait for my go-ahead before writing any code.
```

#### STEP P1 — 任务时间线（Read → Plan → Edit → Test → Summary）
```text
STEP P1 — Task timeline. CSS/frontend-only, keep all existing IDs/classes/bridge
handlers intact.

Placement: a single-row, compact progress strip at the top of the AI's current turn
(above its message bubble), not a separate panel.

Stages (derive from existing activity events per P0's findings):
  Read (file reads) → Plan (reasoning/planning text) → Edit (write/edit calls) →
  Test (bash/test runs) → Summary (final answer text)

Visual spec (match existing neutral theme — do not introduce new accent colors):
- Layout: horizontal row of 5 steps, each = [dot] label, connected by a thin 1px line.
  Dark theme line: rgba(255,255,255,.12). Light theme line: rgba(0,0,0,.12).
- Completed step: dot = 14px filled circle in the primary text color
  (dark #e8e8e8 / light #1a1a1a), with a small monochrome checkmark glyph inside
  (use currentColor, no colored icon). Label text: muted color (#a0a0a0 dark / #666 light).
- Current step: dot = 14px filled circle, same primary color, with a subtle pulse
  animation (opacity 0.6 → 1 → 0.6 over 1.4s, ease-in-out, infinite). Label text:
  primary color (#e8e8e8 dark / #1a1a1a light), font-weight 500 (not 700 — project
  uses only 400/500).
- Upcoming step: dot = 14px outline circle only (1px border, muted color, transparent
  fill). Label text: muted/faded color (#6e6e6e dark / #999 light).
- Connecting line between two completed/current steps: primary-ish (rgba at 40%
  opacity); between current and upcoming: the default faint 12% line.
- Typography: labels 12px, letter-spacing .04em, same treatment as existing all-caps
  section labels (SESSIONS / STUDIO). Font: Inter, weight 400/500 only.
- Spacing: 8px gap between dot and label, 20px gap between step groups. Row padding
  12px vertical, sits in its own row with 8px margin below before the message text.
- Do NOT use color (green/red/blue) for step states — reserve color exclusively for
  diff add/remove lines elsewhere in the app. This timeline stays monochrome.

Verify live: trigger a real multi-step task and confirm the strip updates step-by-step
in both light and dark themes. Report before/after screenshots description.
```

#### STEP P2 — 实时状态小标签
```text
STEP P2 — Status chip. CSS/frontend-only.

Placement: small pill, inline right next to (or just above) the composer/input box,
OR floating just above the current AI message — pick whichever existing container is
least disruptive to current layout; tell me which you chose.

Content examples: "Reading files…", "Running checks…", "Waiting for your review".
Drive text from the same activity events as P1. Hide completely (not just empty) when
idle/no active turn.

Visual spec:
- Shape: pill, border-radius 999px, padding 4px 10px.
- Background: surface-elevated color (dark #262626 / light #f0f0f0).
- Border: 1px solid, 8% opacity (rgba(255,255,255,.08) dark / rgba(0,0,0,.08) light).
- Text: 12px, weight 400, color = secondary (#a0a0a0 dark / #666 light).
- Leading indicator: a small 6px dot before the text, monochrome, with a soft breathing
  animation (opacity 0.4 → 1 → 0.4, 1.2s loop) — same visual language as P1's current-step
  dot, so the two feel like one system, not two different widgets.
- No colored background (no blue/purple "info chip" style) — keep it neutral grey,
  consistent with the rest of the app.
- Transition: fade in/out (opacity + 4px translateY) over 150ms when it appears/disappears
  or changes text, not an abrupt swap.

Verify live in both themes; report where you placed it and why.
```

#### STEP P3 — 运行中会话的小圆点
```text
STEP P3 — Running indicator on sessions list. CSS/frontend-only.

Placement: small dot to the right of the session title in the left sidebar list, only
for the session that currently has an active/generating turn (use the same "active
turn" signal identified in P0).

Visual spec:
- Dot: 6px circle, monochrome — same primary text color as the rest of that row
  (not a colored "online" dot). Reasoning: this app's accent budget is nearly all
  spent on the diff red/green; adding a colored live-dot would break that restraint.
- Animation: gentle pulse, opacity 0.5 → 1 → 0.5, 1.4s ease-in-out infinite — reuse
  the exact same keyframe as P1/P2 so all three "activity" indicators feel identical.
- Position: 8px to the right of the session title text, vertically centered with it.
- Remove immediately (no fade delay) when that session's turn finishes.
- Only one session should ever show the dot at a time (or however many are truly
  concurrently active, per whatever the backend actually supports today).

If you'd rather use a subtle color for this one instead of monochrome (some users find
an all-grey "is it running?" dot ambiguous vs a genuine `#3a3a3a`-on-`#3a3a3a` dot in
dark mode) — flag that trade-off to me before deciding; default to monochrome per the
existing style guide unless I say otherwise.

Verify live: start a task, confirm the dot appears next to the right session and clears
on completion, in both themes.
```

#### STEP P4 — 侧边栏文件夹/日期分组
```text
STEP P4 — Sidebar folder grouping. CSS/frontend-only, do not lose any existing session
click/select/delete behavior.

Effect: group the session list under collapsible section headers. If there's no real
"workspace/folder" grouping data available in the frontend today, group by recency
instead: Today / Yesterday / Earlier (compute from each session's existing timestamp
field — do not add new backend fields).

Visual spec:
- Section header: 12px, uppercase, letter-spacing .04em, color = muted
  (#6e6e6e dark / #999 light) — identical treatment to the existing SESSIONS/STUDIO
  labels, so it reads as "the same kind of label," not a new UI element.
- Collapse chevron: small monochrome stroke icon (currentColor), 12px, rotates 90deg
  on collapse/expand, 150ms ease transition. Sits to the left of the header text.
- Header row: 8px vertical padding, clickable across full row width (not just the
  chevron) to toggle collapse.
- Spacing: 16px gap above each new group header (below the previous group's last
  item), 6px gap between header and its first session row.
- Collapsed state: persist per-group in localStorage (or existing session-storage
  mechanism if the app has one) so it survives a refresh.
- No border boxes around groups — separation is via spacing + the label only, per
  the existing "borders are rare, whitespace does the separating" rule.

Verify live in both themes; confirm existing session interactions (select/new
chat/delete) still work unchanged inside the grouped list.
```

#### STEP P5 — 先调查:跨文件夹访问现在是什么行为
```text
STEP P5 — Investigate cross-folder access (do NOT build UI yet):
- Today, if the AI (via its file tools) tries to read/write a path outside the
  session's current working folder, what actually happens? Trace and report:
  1. Is it silently allowed, silently blocked, or does some existing guard/permission
     layer intercept it (recall: A2 in the backlog found that a "silent failure" was
     actually a permission/guard block, not a frontend bug — check for the same
     pattern here)?
  2. If there IS a guard, what event/callback does the frontend receive when it fires
     (name, payload, does it currently show anything to the user or fail silently)?
  3. Is "current working folder" tracked per-session already, and is there an existing
     concept of an "approved folder list" for a session?
- Do not implement the permission dialog yet. Report findings and wait for my go-ahead.
```

#### STEP P6 — 跨文件夹访问权限弹窗（好看的那种）
```text
STEP P6 — Folder access permission dialog. Wire to the real guard/event found in P5
(do not fake it with a client-side-only mock). Frontend styling should match the
existing neutral Codex/Claude theme exactly — reuse existing CSS variables for colors,
do not hardcode new ones if equivalents already exist.

Trigger: when the AI attempts to access a path outside the session's current approved
folder(s), pause that action and show a modal asking the user to approve or deny.

Visual spec:
- Overlay: full-screen scrim, background rgba(0,0,0,.45), backdrop-filter: blur(2px)
  if supported, click-outside does NOT dismiss (this is a security decision, not a
  casual dropdown — require an explicit button choice).
- Dialog card: centered, max-width 420px, border-radius 12px, background = the
  existing "elevated surface" color (dark #262626 / light #ffffff), border 1px at
  8% opacity, shadow: soft only — 0 8px 30px rgba(0,0,0,.25) dark / rgba(0,0,0,.12)
  light (no hard/heavy shadow).
- Icon: a small 32px monochrome outline folder icon at the top, inside a 48px circle
  with a faint neutral background (rgba(255,255,255,.06) dark / rgba(0,0,0,.04) light).
  Do NOT use a red/orange warning color for the icon — keep it neutral so it matches
  the rest of the app; the text content carries the "this needs your attention"
  weight, not alarm color.
- Title: "Allow access to this folder?" — 15px, weight 500, primary text color.
- Body: 13px, secondary color (#a0a0a0 dark / #666 light), line-height 1.6. Show the
  actual path being requested in a monospace inline chip below the sentence:
  font-family "JetBrains Mono", background rgba(255,255,255,.06) dark /
  rgba(0,0,0,.04) light, border-radius 4px, padding 2px 6px, 12.5px size, truncate
  the middle of long paths (e.g. "/Users/.../Project/other-repo") with a tooltip
  showing the full path on hover.
- Optional reason line (if the AI provides one via the P5 event payload): a short
  italic sentence, e.g. "Needed to check a shared config file." — same secondary color.
- Checkbox: "Allow for the rest of this session" — 13px, unchecked by default,
  standard neutral checkbox matching the existing Feature Connections checkbox style
  already in the app (reuse that component/CSS, don't build a new checkbox style).
- Actions (bottom row, right-aligned, 12px gap):
  - "Deny" — secondary/ghost button: transparent background, 1px border at 12%
    opacity, text = primary color, 8px 16px padding, border-radius 8px.
  - "Allow" — primary button using the app's EXISTING primary button style (the
    monochrome dark/light button already used for "New chat"/Send — do not invent a
    new button color).
- Keyboard: Esc = Deny, Enter = Allow (focus starts on Allow since approving is the
  common case, but make Deny reachable by a single Tab).
- Animation: dialog scales in from 0.96 → 1 with opacity 0 → 1, 150ms ease-out. No
  bounce/spring.

Behavior:
- Block the pending file operation until the user responds (show it as "Waiting for
  your review" via the existing P2 status chip while the dialog is open).
- On Allow: proceed with the original operation, and if the checkbox was checked,
  remember this folder as approved for the rest of the session (used to avoid
  re-prompting for the same folder).
- On Deny: cancel that specific operation only, tell the AI it was denied (so it can
  explain to the user in its own reply why it couldn't complete that step), and do NOT
  crash or silently hang.

Verify live: actually get the AI to attempt an out-of-scope path and confirm the real
dialog appears (not a mock), both Allow and Deny paths work, and the "allow for rest of
session" checkbox is respected on a second attempt. Report the exact event/guard you
wired this to from P5.
```

#### STEP P8 — 先调查:消息/历史现在怎么存的
```text
STEP P8 — Investigate before building the quote feature (do NOT edit yet):
- I want users to select text inside any past message (AI or user) in the chat feed,
  and quote it into the composer to ask a follow-up anchored to that exact snippet —
  like Claude.ai's "select text → Quote button → inserted above composer" pattern.
- Report:
  1. How messages are currently rendered in the DOM — is each message a discrete
     element with a stable id/data attribute I can reference (e.g. data-message-id),
     or is it just raw HTML dumped into a shared container with no per-message anchor?
  2. Whether message content is rendered from plain text/markdown (so a substring can
     be cleanly extracted) or from something that makes arbitrary text-selection
     fragile (e.g. virtualized/re-rendered lists, streaming text that mutates the DOM
     while the user is trying to select it).
  3. What the composer/input component looks like today — is it a plain <textarea>,
     or a rich element where I could inject a styled "quoted block" above the typed
     text (not just prepend plain text into the textarea, which would look ugly)?
  4. Whether selecting text mid-stream (while the AI is still generating that message)
     is even feasible, or whether quoting should only be enabled on completed messages.
- Do not implement anything yet. Report findings and any constraints, then wait for my
  go-ahead — the design in P9 depends on what's actually feasible here.
```

#### STEP P9 — Quote 功能（选中文字 → 引用 → 续问）
```text
STEP P9 — Quote-to-reply feature. Frontend-only, keep bridge/backend/existing chat
logic fully intact. Build on what P8 found; if P8 found streaming selection is
unreliable, restrict quoting to completed (non-streaming) messages only and tell me.

Interaction:
1. User selects text inside any past message (AI or user).
2. A small floating "Quote" button appears near the selection (standard text-selection
   popup pattern — position above the selection, flip below if it would go off-screen).
3. Clicking it inserts the selected text as a distinct QUOTED BLOCK attached to the
   composer (not just pasted into the textarea as plain text), positioned directly
   above where the user types their follow-up. The user then types normally below it
   and hits Send; the quote travels with that one message only.
4. A small "×" on the quote block lets the user remove it before sending (cancel the
   quote without canceling their typed draft).
5. Only one active quote at a time is fine for v1 — if the user selects new text while
   a quote is already pending, replace it (don't stack multiple quotes).

Visual spec (reuse existing theme tokens, no new hardcoded colors):
- Selection "Quote" button: small pill, 12px text, padding 4px 10px, background =
  elevated surface (dark #262626 / light #ffffff), border 1px at 8% opacity, subtle
  shadow (0 4px 12px rgba(0,0,0,.15)), border-radius 8px. Icon: small monochrome
  quote-mark glyph (currentColor) before the label. Appears with a 100ms fade+4px
  translateY, no bounce.
- Quoted block (in the composer, above the typed-text area): left border 2px solid
  in the primary text color (dark #e8e8e8 / light #1a1a1a) — NOT a colored accent,
  consistent with the app's "color reserved for diff only" rule. Background = surface
  (dark #202020 / light #f7f7f7), border-radius 6px on the non-bordered corners,
  padding 8px 10px, margin-bottom 6px before the textarea.
  - Quoted text: 13px, secondary color (#a0a0a0 dark / #666 light), line-height 1.5,
    max 3 lines with ellipsis overflow (long quotes shouldn't dominate the composer).
  - Small label above the quoted text, 11px, muted (#6e6e6e dark / #999 light),
    uppercase, letter-spacing .04em: "Replying to [You / AI Studio]" depending on
    who the quoted message was from.
  - "×" remove button: top-right of the block, monochrome icon button, 20px hit
    target, subtle hover background (rgba(255,255,255,.06) dark / rgba(0,0,0,.04)
    light).
- In the SENT message bubble (after sending), render the quote the same way — a
  small quoted-block excerpt above the new message text, same left-border style but
  slightly more compact (2 lines max, 12px text).
- Clicking the quoted excerpt inside a past sent message scrolls the chat back to and
  briefly highlights (150ms background flash, then fade) the ORIGINAL source message
  it was quoted from — like Slack's "jump to parent." Skip this if P8 found there's no
  stable per-message anchor to scroll to; tell me instead of faking it.

Keep language plain (no "DOM node", "selection range" etc. shown to the user — button
just says "Quote").

Verify live: select text in an old AI message, quote it, type a follow-up, send it,
confirm the quote renders correctly in the sent bubble in both light and dark themes,
and confirm removing a pending quote before sending works. Report any P8 constraints
that changed the final behavior (e.g. streaming-message quoting disabled).
```

#### STEP P16 — 先调查:现在能不能支持多个本地文件夹
```text
STEP P16 — Investigate before building multi-folder support (do NOT edit yet):
- Today the session has ONE working folder (the FOLDER path shown in the composer
  area). I want users to add MULTIPLE local folders to a session, like Claude's own UI
  (a row of folder chips above the composer, plus a small "add folder" icon button —
  see reference screenshot).
- Report:
  1. Is "current working folder" a single hardcoded path per session in the backend/
     bridge, or could it already accept a list? Where is this defined (file/line)?
  2. What native folder-picker API does the current Python/Qt stack use (e.g. Qt's
     QFileDialog.getExistingDirectory only returns ONE folder per call) — can the user
     select multiple folders in a single dialog, or would adding several folders
     require clicking "add folder" multiple times (once per folder)? Report which is
     actually feasible, don't assume.
  3. How does the agent's file-read/search/edit tool layer resolve paths today — does
     it accept a list of allowed root directories, or only a single root? If only a
     single root, is extending it to a list a small change or a large one? Report your
     honest assessment.
  4. This is the same underlying question as P5 (cross-folder access) — if P5 was
     already answered, reuse those findings here instead of re-investigating from
     scratch; report how the two connect (e.g. folders added via this new "+folder"
     chip should presumably auto-count as "approved" for P6's permission dialog,
     avoiding a redundant prompt for a folder the user just explicitly added).
- Do not implement anything yet. Report findings and wait for my go-ahead.
```

#### STEP P17 — "+文件夹" chip UI（可添加多个本地文件夹）
```text
STEP P17 — Add-folder chips UI, matching Claude's compact pattern. Build on P16's
findings — if multiple folders aren't feasible without backend changes, say so clearly
before building a UI that can't actually be backed by real functionality.

Placement: a single-line row of small pills above the composer (reuse/extend the same
area touched in A10's composer simplification — don't create a second, separate
header row if A10 already restructured this area).

Visual spec (reuse existing neutral theme tokens):
- Each folder pill: small rounded pill, ~28px tall, padding 6px 10px, background =
  surface color (dark #202020 / light #ffffff), border 1px at 8% opacity, 13px text,
  folder icon (monochrome, currentColor) before the label. Long folder names truncate
  with an ellipsis in the middle; full path shown in a tooltip on hover.
- Remove affordance: a small "×" appears at the right of the pill ONLY on hover (keep
  pills clean/minimal at rest, like Claude's). Clicking it removes that folder from
  the session's accessible scope — do not just hide it visually, actually revoke access
  (per P16's findings on how scope is enforced).
- "Add folder" button: small square icon button at the end of the row — folder icon
  with a small "+" badge in the corner (see reference screenshot), same monochrome
  icon-button style used elsewhere in the app (subtle hover background, no color).
  Clicking it opens the native folder picker; each successful selection appends a new
  pill. If the same folder is picked twice, don't add a duplicate pill.
- If many folders are added, the row should scroll horizontally rather than wrap and
  grow the header's height — keep this area compact and single-line.
- If P16 found that only ONE folder can realistically be supported without deeper
  backend changes, propose the smallest honest version instead of faking multi-folder
  support: e.g., keep the single current-folder pill exactly as today, and make the
  "+folder" button either (a) disabled with a tooltip explaining it's not supported
  yet, or (b) a "switch folder" action (replaces the current folder) rather than
  "add." Tell me which you implemented and why.

Cross-reference P5/P6 if built: a folder added here should be treated as pre-approved,
so the AI accessing it later does NOT trigger the P6 permission dialog again for that
same path.

Verify live: add a second folder via the button, confirm the AI can actually read/
search files from BOTH folders afterward (not just visually show two pills), remove
one, and confirm access to the removed folder is actually revoked. Report the real
end-to-end behavior, not just the UI appearance.
```

### C. 暂缓(需后端支持,当前后端不支持)
- [ ] 多窗口 / 多会话**真并行** —— 等后端具备并发能力后再立项

## 收官建议
1. 完成"进行中"三项 + 清 A(含 A4 黑色 bar、A6 长行截断、A7 滚动条卡顿)→ 可 demo 的
   完整产品。A5(主窗口深蓝边框)风险较高(动窗口壳层),建议排在最后,单独发、单独验证。
2. 之后按需做 B(体验升级,不做也完整;P5/P6 权限弹窗除外,建议尽早查清楚)。
3. C 等后端。
